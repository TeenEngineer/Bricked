Place a square PNG icon at `assets/app_icon/app_icon.png` (1024x1024 recommended).

Then regenerate launcher icons with:

`dart run flutter_launcher_icons`

This project is already configured for Android and iOS icon generation in `pubspec.yaml`.
